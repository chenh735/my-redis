# 压力测试实现 Todo

## 实现思路
- 新增独立二进制 `stress`，通过 TCP 连接真实服务端，复用现有 RESP 编码/解码逻辑。
- 支持多 client 并发、总请求数、pipeline 批量发送、key 空间、value 大小和 workload 参数。
- 默认使用 `PING` 做低副作用压测，也支持 `SET`、`GET`、`MIXED` 覆盖读写路径。
- 汇总输出总请求数、成功数、失败数、耗时、QPS、平均/最小/最大耗时和 P50/P95/P99。
- 尽量不改服务端、数据库、命令分发等已有功能代码。

## Todo
- ✅ 新增 `src/bin/stress.rs` 压测入口。
- ✅ 实现命令行参数解析。
- ✅ 实现请求分配、请求生成和 pipeline 执行。
- ✅ 实现压测结果聚合和统计输出。
- ✅ 补充压测工具相关单元测试。
- ✅ 运行格式化、测试和构建验证。
- ✅ 复查本清单，确认功能均已实现。

## 高级功能压测扩展 Todo
- ✅ 新增 List / Set / Hash / Advanced workload。
- ✅ 覆盖 List 命令：LPUSH、RPUSH、LPOP、RPOP、LLEN、LRANGE。
- ✅ 覆盖 Set 命令：SADD、SREM、SISMEMBER、SCARD、SMEMBERS、SINTER、SUNION、SDIFF。
- ✅ 覆盖 Hash 命令：HSET、HGET、HDEL、HEXISTS、HLEN、HKEYS、HVALUES、HGETALL。
- ✅ 覆盖常用基础命令：PING、ECHO、SET、GET、STRLEN、APPEND、DEL、EXISTS。
- ✅ 补充命令生成单元测试。
- ✅ 运行格式化、测试、构建和端到端验证。

## 日志功能 Todo
- ✅ 新增 logger 模块，封装日志初始化和日志级别配置。
- ✅ 服务端新增命令行参数，支持通过命令决定是否开启日志。
- ✅ 将服务端关键错误和生命周期事件接入日志。
- ✅ 为关键功能函数添加简短注释。
- ✅ 补充日志级别和服务端参数解析测试。
- ✅ 更新 README 和模块 README 文档。
- ✅ 运行格式化、测试和构建验证。
- ✅ 复查本清单，确认日志功能均已实现。

## TCP 空闲连接超时 Todo
- ✅ 在配置中新增空闲连接超时时间，默认开启，`0` 表示禁用。
- ✅ 服务端新增命令行参数覆盖空闲连接超时时间。
- ✅ 服务端读取请求时检测空闲超时，并主动断开长时间无请求连接。
- ✅ 为关键读取函数添加简短注释。
- ✅ 补充配置解析、参数解析和空闲超时读取测试。
- ✅ 更新 README 和模块 README 文档。
- ✅ 运行格式化、测试和构建验证。
- ✅ 复查本清单，确认空闲连接超时功能均已实现。

## 客户端多命令 Todo
- ✅ 阅读 `src/bin/client.rs` 和 RESP 编解码逻辑。
- ✅ 保留 `--cmd` 单条命令模式，新增不传 `--cmd` 时的交互模式。
- ✅ 在同一个 TCP 连接中循环发送多条命令并读取响应。
- ✅ 支持输入 `exit` 主动断开 TCP 连接。
- ✅ 为关键函数添加简短注释。
- ✅ 补充客户端多命令相关测试。
- ✅ 运行格式化、测试、构建和端到端验证。
- ✅ 复查本清单，确认客户端多命令功能均已实现。
